import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  table: {
    paddingTop: '50px',
    marginLeft: 'auto',
    marginRight: 'auto',
  },
  summary: {
    marginTop: '20px',
    marginBottom: '50px',
  },
}));

export default useStyles;
