import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  table: {
    paddingTop: '30px',
    marginLeft: 'auto',
    marginRight: 'auto',
  },
  summary: {
    alignItems: 'right',
  },
  clearBtn: {
    marginTop: '20px',
  },
}));

export default useStyles;
