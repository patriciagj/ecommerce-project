import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  form: {
    marginLeft: 'auto',
    marginRight: 'auto',
    textAlign: 'center',
    paddingTop: '100px',
    paddingBottom: '100px',
    maxWidth: 250,
  },
}));

export default useStyles;
