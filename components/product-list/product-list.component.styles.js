import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  grid: {
    textAlign: 'center',
  },
  content: {
    display: 'flex',
  },
}));

export default useStyles;
